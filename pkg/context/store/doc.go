// Package contextstore validates the served-repo context branch tree
// (meta.yaml, chronology.md, and required dossier files).
//
// This is the open runner half of the context handshake: schema and validation
// only. Creating or evolving teaching story and agenting packs belongs to the
// private majordomo-context factory. Review jobs attach packs after validate;
// they must not invent rejected candidates, scores, or digest stage traces.
package contextstore
