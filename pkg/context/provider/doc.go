// Package contextprovider resolves finished repo-context packs for review runs.
//
// The open runner consumes context through ContextProvider implementations.
// Private majordomo-context publishes finished packs; it is not imported here.
package contextprovider
